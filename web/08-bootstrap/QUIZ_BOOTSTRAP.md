# Quiz — Bootstrap 5

## Domanda 1

Qual è lo scopo principale della classe `.container`?

A. Creare automaticamente una griglia a 12 colonne.  
B. Centrare il contenuto e assegnargli una larghezza massima responsive.  
C. Applicare automaticamente margini e padding a tutti gli elementi figli.  
D. Rendere il layout responsive senza bisogno della grid.

---

## Domanda 2

Quale affermazione descrive correttamente Bootstrap come framework?

A. Bootstrap sostituisce completamente HTML e CSS.  
B. Bootstrap è una raccolta di componenti JavaScript da richiamare con funzioni.  
C. Bootstrap mette a disposizione classi CSS componibili e alcuni componenti che richiedono JavaScript.  
D. Bootstrap serve esclusivamente a creare siti responsive.

---

## Domanda 3

Dato `col-12 col-md-6`, quale comportamento ottieni?

A. Su mobile i blocchi stanno affiancati al 50%.  
B. Su mobile ogni blocco occupa tutta la riga; da `md` in su occupa metà riga.  
C. I blocchi occupano sempre metà riga.  
D. Da `md` in su ogni blocco occupa tutta la riga.

---

## Domanda 4

Cosa sono i gutter in Bootstrap?

A. I bordi automatici delle card.  
B. Gli spazi tra colonne e righe della grid.  
C. Le classi usate per cambiare colore ai bottoni.  
D. I breakpoint responsive della navbar.

---

## Domanda 5

Quale combinazione crea un vero bottone Bootstrap principale?

A. `primary`  
B. `btn-primary`  
C. `btn btn-primary`  
D. `button btn-primary`

---

## Domanda 6

Quale componente richiede sicuramente il JavaScript di Bootstrap?

A. Card  
B. Container  
C. Modal  
D. Grid

---

## Domanda 7

Dato `col-12 col-md-6 col-lg-4`, quanti elementi per riga ottieni?

A. Mobile: 2 · Tablet: 3 · Desktop: 4  
B. Mobile: 1 · Tablet: 2 · Desktop: 3  
C. Mobile: 3 · Tablet: 2 · Desktop: 1  
D. Sempre 3 elementi per riga.

---

## Domanda 8

Quale affermazione descrive meglio la filosofia di Bootstrap?

A. È meglio imparare a memoria tutte le classi Bootstrap.  
B. Bootstrap serve principalmente a evitare completamente l'uso del CSS.  
C. Bootstrap è composto da piccoli mattoncini, utility, layout e componenti, che si combinano tra loro.  
D. Bootstrap funziona solo se si utilizzano i temi Bootswatch.

---

## Domanda 9

Quale affermazione sui breakpoint Bootstrap è corretta?

A. `col-md-6` vale solo per gli schermi di dimensione `md`.  
B. `col-md-6` vale da `md` in su, finché non viene sostituita da una classe più specifica.  
C. `col-md-6` vale solo su desktop.  
D. I breakpoint Bootstrap funzionano in modalità desktop-first.

---

## Domanda 10

Qual è il ruolo di `card-body`?

A. Inserisce automaticamente un bordo attorno alla card.  
B. Definisce il contenitore interno della card, gestendo il padding del contenuto.  
C. Rende la card responsive.  
D. Permette di inserire immagini nella parte superiore della card.

---

## Domanda 11

A cosa serve `card-img-top`?

A. A mettere un'immagine sopra la card come parte del componente.  
B. A trasformare una card in una galleria immagini automatica.  
C. A rendere tutte le immagini rotonde.  
D. A far aprire l'immagine in una modal.

---

## Domanda 12

Quale affermazione descrive correttamente Bootswatch?

A. È un framework alternativo a Bootstrap con classi completamente diverse.  
B. È un insieme di temi che cambia l'aspetto di Bootstrap senza modificare il markup HTML.  
C. Serve ad aggiungere il JavaScript mancante ai componenti Bootstrap.  
D. Permette di eliminare completamente il file CSS di Bootstrap.

---

## Domanda 13

Perché Bootstrap usa la classe `form-control` sugli input?

A. Per validare automaticamente i dati inseriti.  
B. Per applicare uno stile coerente agli input, senza modificarne il comportamento HTML.  
C. Per collegare automaticamente la label all'input.  
D. Per rendere obbligatorio il campo.

---

## Domanda 14

Dato `form-control is-invalid` con `invalid-feedback`, quale affermazione è corretta?

A. Bootstrap valida automaticamente il contenuto dell'input.  
B. `is-invalid` indica solo lo stato visivo del campo; la validazione vera deve arrivare da HTML5, JavaScript o backend.  
C. `invalid-feedback` sostituisce completamente la validazione del browser.  
D. `invalid-feedback` funziona solo con Bootswatch.

---

## Domanda 15

Nei radio button HTML, cosa permette di scegliere una sola opzione tra più radio?

A. La classe `form-check`.  
B. La classe `form-check-input`.  
C. Lo stesso valore dell'attributo `name` su più radio.  
D. La classe `form-check-label`.

---

## Domanda 16

Perché una navbar Bootstrap responsive ha bisogno del JavaScript di Bootstrap?

A. Per colorare automaticamente la navbar.  
B. Per rendere responsive la griglia.  
C. Per gestire apertura e chiusura del menu hamburger tramite collapse.  
D. Per caricare automaticamente i link della navigazione.

---

## Domanda 17

Quale componente NON richiede il JavaScript di Bootstrap?

A. Modal  
B. Carousel  
C. Card  
D. Accordion

---

## Domanda 18

Qual è il vantaggio principale di Bootswatch?

A. Aggiunge nuovi componenti come Modal e Carousel.  
B. Cambia l'aspetto grafico dell'applicazione mantenendo invariato il markup Bootstrap.  
C. Sostituisce il JavaScript di Bootstrap con una versione più veloce.  
D. Permette di eliminare completamente le classi Bootstrap.

---

## Domanda 19

Stai costruendo un mini sito vetrina Bootstrap. Quale sequenza segue meglio il percorso studiato?

A. Footer → Modal → Hero → Navbar → Contact  
B. Navbar → Hero → Features → Contact → Footer  
C. Hero → Accordion → Carousel → Footer → Navbar  
D. Contact → Hero → Grid → Buttons → Footer

---

## Domanda 20

Secondo la filosofia studiata, qual è il modo corretto di imparare Bootstrap?

A. Memorizzare il maggior numero possibile di classi CSS.  
B. Copiare componenti da Internet e modificarli finché funzionano.  
C. Capire prima la grammatica di Bootstrap, layout, utility e componenti, poi imparare a combinarli.  
D. Usare sempre Bootswatch, così non serve conoscere Bootstrap.

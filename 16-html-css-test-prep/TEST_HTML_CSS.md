# Pre-test HTML/CSS

## Obiettivo

Questa esercitazione serve per ripassare i concetti HTML/CSS visti finora in vista del test a risposta multipla.

Non è un test ufficiale.

È una palestra di ripasso.

## Istruzioni

Per ogni domanda scegli una sola risposta corretta.

Le soluzioni commentate sono in fondo al documento.

---

# Domande

## 1. A cosa serve principalmente HTML?

A. A definire colori e font della pagina  
B. A descrivere struttura e contenuto della pagina  
C. A salvare dati nel database  
D. A creare logica lato server  

## 2. A cosa serve principalmente CSS?

A. A descrivere la struttura della pagina  
B. A definire aspetto, stile e disposizione degli elementi  
C. A inviare dati al backend  
D. A creare variabili C#  

## 3. Quale tag contiene normalmente il contenuto visibile della pagina?

A. `<head>`  
B. `<title>`  
C. `<body>`  
D. `<style>`  

## 4. Quale tag viene usato per creare un link?

A. `<link>`  
B. `<a>`  
C. `<url>`  
D. `<href>`  

## 5. Cosa fa `target="_blank"`?

A. Apre il link in una nuova scheda o finestra  
B. Apre il link nella stessa pagina  
C. Rende il link invisibile  
D. Elimina il link  

## 6. Quale tag crea una lista non ordinata?

A. `<ol>`  
B. `<ul>`  
C. `<li>`  
D. `<list>`  

## 7. Quale tag rappresenta un elemento di lista?

A. `<item>`  
B. `<element>`  
C. `<li>`  
D. `<ul>`  

## 8. Quale tag crea una tabella?

A. `<table>`  
B. `<tr>`  
C. `<td>`  
D. `<th>`  

## 9. In una tabella, cosa rappresenta `<tr>`?

A. Una cella  
B. Una riga  
C. Una colonna  
D. Una intestazione  

## 10. In una tabella, cosa rappresenta `<td>`?

A. Una cella dati  
B. Una riga  
C. Una tabella intera  
D. Una lista  

## 11. In una tabella, cosa rappresenta `<th>`?

A. Una cella di intestazione  
B. Una cella nascosta  
C. Una riga vuota  
D. Un bordo  

## 12. A cosa serve `colspan`?

A. A unire più righe  
B. A unire più colonne  
C. A colorare una cella  
D. A cancellare una tabella  

## 13. A cosa serve `rowspan`?

A. A unire più righe  
B. A unire più colonne  
C. A centrare il testo  
D. A creare un link  

## 14. Quale selettore CSS seleziona tutti gli elementi?

A. `#`  
B. `.`  
C. `*`  
D. `all`  

## 15. Cosa seleziona `.box`?

A. Tutti gli elementi con id `box`  
B. Tutti gli elementi con classe `box`  
C. Tutti i tag `<box>`  
D. Tutti i link  

## 16. Cosa seleziona `#header`?

A. Tutti gli elementi con classe `header`  
B. L'elemento con id `header`  
C. Tutti i tag `<header>`  
D. Tutte le tabelle  

## 17. Quale proprietà CSS crea spazio interno?

A. `margin`  
B. `padding`  
C. `float`  
D. `clear`  

## 18. Quale proprietà CSS crea spazio esterno?

A. `margin`  
B. `padding`  
C. `border`  
D. `font-size`  

## 19. Nel box model, quale ordine è corretto?

A. contenuto, padding, bordo, margine  
B. margine, contenuto, link, form  
C. id, class, name, value  
D. body, head, html, title  

## 20. A cosa serve `border`?

A. A creare un bordo attorno a un elemento  
B. A inviare una form  
C. A creare una lista  
D. A selezionare un id  

## 21. Cosa fa `float:left`?

A. Nasconde un elemento  
B. Sposta/affianca un elemento verso sinistra nel flusso  
C. Rende obbligatorio un campo  
D. Cambia il colore del testo  

## 22. A cosa serve `clear:both`?

A. A cancellare tutti i dati  
B. A interrompere il flusso degli elementi flottanti  
C. A creare una nuova tabella  
D. A rendere un campo obbligatorio  

## 23. Cosa significa il selettore `table a`?

A. Seleziona tutti i link dentro una tabella  
B. Seleziona tutte le tabelle dentro un link  
C. Seleziona tutte le tabelle e tutti i link separatamente  
D. Seleziona tutti gli elementi della pagina  

## 24. Cosa significa “scope del selettore”?

A. Che una regola CSS vale solo in un certo contesto  
B. Che una form salva dati automaticamente  
C. Che una tabella ha molte righe  
D. Che un link apre una nuova scheda  

## 25. A cosa serve il tag `<div>`?

A. A creare un contenitore generico  
B. A inviare dati al server  
C. A creare solo immagini  
D. A creare solo tabelle  

## 26. A cosa serve il tag `<form>`?

A. A creare una tabella  
B. A raggruppare campi e controlli per l'invio dati  
C. A creare una lista  
D. A caricare un'immagine  

## 27. Quale attributo collega una `label` al suo campo?

A. `class`  
B. `for`  
C. `href`  
D. `src`  

## 28. Quale attributo identifica il dato inviato al backend?

A. `id`  
B. `class`  
C. `name`  
D. `style`  

## 29. Qual è la differenza principale tra `id` e `name` in una form?

A. `id` serve al browser/CSS/JS, `name` serve all'invio del dato  
B. Sono sempre identici e intercambiabili  
C. `name` serve solo per il colore  
D. `id` serve solo per il database  

## 30. Perché i radio button dello stesso gruppo devono avere lo stesso `name`?

A. Per avere lo stesso colore  
B. Per permettere una sola scelta tra più opzioni  
C. Per essere più grandi  
D. Per diventare tutti obbligatori  

## 31. Quando è corretto usare una checkbox?

A. Quando bisogna confermare una condizione sì/no  
B. Quando bisogna scegliere una sola opzione tra molte  
C. Quando bisogna scrivere testo lungo  
D. Quando bisogna creare una tabella  

## 32. Quale tag è più adatto per inserire testo lungo?

A. `<input type="text">`  
B. `<textarea>`  
C. `<select>`  
D. `<button>`  

## 33. A cosa serve `<select>`?

A. A scegliere un valore da una lista  
B. A scrivere testo lungo  
C. A creare un bordo  
D. A unire celle di tabella  

## 34. A cosa serve `required`?

A. A rendere obbligatoria la compilazione di un campo  
B. A cambiare il colore del campo  
C. A salvare dati sul server  
D. A creare una nuova pagina  

## 35. Cosa fa `type="reset"`?

A. Invia la form  
B. Cancella o ripristina i valori della form  
C. Crea un link  
D. Aggiunge un bordo  

## 36. Cosa fa `type="submit"`?

A. Invia la form  
B. Cancella la form  
C. Cambia il CSS  
D. Crea una tabella  

## 37. Una form HTML da sola salva i dati?

A. Sì, sempre  
B. No, serve un backend o una destinazione di invio  
C. Sì, se ha un bottone submit  
D. Sì, se contiene una checkbox  

## 38. Perché è meglio usare CSS invece di tanti `<br>` per creare spazi?

A. Perché la spaziatura visuale è responsabilità del CSS  
B. Perché `<br>` non esiste  
C. Perché CSS salva dati  
D. Perché HTML non può andare a capo  

## 39. Perché in un esercizio didattico può avere senso NON usare Flexbox o Grid?

A. Perché sono vietati  
B. Perché prima si vogliono capire blocchi, box model, float e clear  
C. Perché non funzionano nei browser  
D. Perché servono solo con JavaScript  

## 40. Quale frase è più corretta?

A. HTML descrive la struttura, CSS descrive la presentazione  
B. HTML descrive i colori, CSS salva i dati  
C. CSS crea il database, HTML crea il server  
D. HTML e CSS sono la stessa cosa  

---

---

## Soluzioni

Le soluzioni commentate sono nel file:

- [SOLUZIONI_HTML_CSS.md](./SOLUZIONI_HTML_CSS.md)
